# Sample Environment Configuration
# Copy this to .env and configure for your setup

# User/Group IDs (find with: id $USER)
PUID=1000
PGID=1000
TZ=America/New_York

# Database credentials
DB_USER=jellystat
DB_PASS=change_this_secure_password
JWT_SECRET=change_this_jwt_secret_to_something_random

# VPN Configuration (configure in deployment/wg0.conf)
# Get your VPN config file from your provider

# Hardware Acceleration (optional - remove if no Intel GPU)
RENDER_GROUP_ID=109

# Optional: Custom URLs
JELLYFIN_PUBLISHED_SERVER_URL=http://localhost:8096
